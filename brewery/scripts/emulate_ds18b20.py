#!/usr/bin/python

# Script to emulate a DS18B20 sensor.
# This will just output a string to a file like a 'real' sensor would do, 
# with the idea being that a development VM for example can then read the 'sensor' without having a real one installed.
